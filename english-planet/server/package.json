{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-all": "node install-all.js",
    "init-db-dev": "cd ./db/db-services && node run-init-db.js dev",
    "init-db-staging": "cd ./db/db-services && node run-init-db.js staging",
    "seed-dev": "knex seed:run --env dev",
    "seed-staging": "knex seed:run --env staging",
    "start-server-dev": "cd ./src && node start-server-dev.js dev",
    "start-server-staging": "cd ./src && node start-server-staging.js staging"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "knex": "^0.95.6",
    "luxon": "^1.28.0",
    "mysql": "^2.18.1",
    "mysql2": "^2.2.5",
    "uuid": "^8.3.2"
  }
}
